using Model;
using Model.Utils;
using Xunit;

namespace Tests.Model
{
	public class PermissionSetTests
	{
		/*public PermissionSetTests()
		{
			set = new PermissionSet<ContestOperation>();
		}

		[Fact]
		public void empty_permission_set_has_no_opinions()
		{
			foreach (var op in EnumUtils.Values<ContestOperation>())
				Assert.False(set.HasOpinionAbout(op));
		}

		[Fact]
		public void doesnt_forget_allowances()
		{
			set.Allow(ContestOperation.View);
			Assert.True(set.HasOpinionAbout(ContestOperation.View));
			Assert.True(set.IsAllowed(ContestOperation.View));
		}

		[Fact]
		public void doesnt_forget_denials()
		{
			set.Deny(ContestOperation.View);
			Assert.True(set.HasOpinionAbout(ContestOperation.View));
			Assert.False(set.IsAllowed(ContestOperation.View));
		}

		readonly PermissionSet<ContestOperation> set;*/
	}
}